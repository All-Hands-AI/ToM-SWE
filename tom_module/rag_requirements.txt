# RAG Agent Dependencies
# Core dependencies for the RAG agent with contextual embeddings

# AI/ML Libraries
anthropic>=0.25.0          # For Claude API (context generation and response generation)
voyageai>=0.2.0           # For high-quality embeddings
numpy>=1.24.0             # For numerical operations and similarity calculations
scikit-learn>=1.3.0       # For additional ML utilities (optional)

# Data Processing
pandas>=2.0.0             # For data manipulation (optional)
tqdm>=4.65.0              # For progress bars during processing

# Optional dependencies for enhanced functionality
cohere>=4.0.0             # For reranking (optional)
elasticsearch>=8.0.0      # For BM25 search (optional)
faiss-cpu>=1.7.0          # For faster similarity search (optional)

# Development and Testing
pytest>=7.0.0             # For testing
pytest-asyncio>=0.21.0    # For async testing
black>=23.0.0             # For code formatting
mypy>=1.0.0               # For type checking

# Note: Install with: pip install -r rag_requirements.txt
# For production use, consider pinning exact versions